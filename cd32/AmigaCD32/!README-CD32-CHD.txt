This folder contains empty files representing CD32 games, just used as a convenient proxy when generating the configs and MGL files. 

You can of course also use this on a directory of real CD32 CHDs, but that's not necessary here, since we just need the file names.
