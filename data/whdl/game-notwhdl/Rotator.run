IF EXISTS WHDSaves:OPTIONS.ROT
  copy WHDSaves:OPTIONS.ROT WHD:N/Rotator QUIET
ENDIF
cd WHD:N/Rotator
ROTATOR3 >NIL:
IF EXISTS WHD:N/Rotator/OPTIONS.ROT
  copy WHD:N/Rotator/OPTIONS.ROT WHDSaves: QUIET
ENDIF
