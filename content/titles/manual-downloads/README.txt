Put manual downloads here and then use `make promote-newer-archives` to update existing archives to the newer versions.

Retired archives will be in the `retired` folders under each category, and can be manually deleted once the updates are verified.
